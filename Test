<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ZemyTools Admin | Sophia Promo</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gray-50 flex h-screen overflow-hidden">

    <aside class="w-64 bg-slate-900 text-white flex flex-col hidden md:flex">
        <div class="p-6 text-2xl font-bold border-b border-slate-800">
            <span class="text-blue-400">Zemy</span>Tools
        </div>
        <nav class="flex-1 p-4 space-y-2">
            <a href="#" class="flex items-center p-3 bg-blue-600 rounded-lg"><i class="fas fa-ticket-alt mr-3"></i> Manage Codes</a>
            <a href="/" class="flex items-center p-3 text-slate-400 hover:bg-slate-800 rounded-lg"><i class="fas fa-external-link-alt mr-3"></i> View Site</a>
        </nav>
        <div class="p-4 border-t border-slate-800">
            <a href="/logout" class="text-red-400 hover:text-red-300 text-sm"><i class="fas fa-sign-out-alt mr-2"></i> Logout</a>
        </div>
    </aside>

    <main class="flex-1 overflow-y-auto">
        <header class="bg-white border-b p-6 flex justify-between items-center shadow-sm">
            <h2 class="text-xl font-semibold text-gray-800">Dashboard Overview</h2>
            <div class="flex items-center gap-3">
                <span class="bg-green-100 text-green-700 px-3 py-1 rounded-full text-xs font-bold uppercase tracking-wider">Live & Secure</span>
            </div>
        </header>

        <div class="p-8">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
                <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-100">
                    <p class="text-gray-500 text-sm uppercase font-bold">Total Codes</p>
                    <h3 class="text-3xl font-bold text-slate-800">{{ codes|length }}</h3>
                </div>
            </div>

            <section class="bg-white p-8 rounded-2xl shadow-sm border border-gray-100 mb-8">
                <h3 class="text-lg font-bold mb-4 text-gray-800">Add New Referral Code</h3>
                <form action="/add" method="POST" class="flex flex-col md:flex-row gap-4">
                    <input type="text" name="name" placeholder="User Name (e.g., John D.)" required 
                           class="flex-1 border p-3 rounded-xl outline-none focus:ring-2 focus:ring-blue-500 transition">
                    <input type="text" name="code" placeholder="Code (e.g., SOPHIA20)" required 
                           class="flex-1 border p-3 rounded-xl font-mono outline-none focus:ring-2 focus:ring-blue-500 transition">
                    <button type="submit" class="bg-blue-600 text-white px-8 py-3 rounded-xl font-bold hover:bg-blue-700 transition">
                        Add Code
                    </button>
                </form>
            </section>

            <div class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden">
                <table class="w-full text-left">
                    <thead class="bg-gray-50 border-b">
                        <tr>
                            <th class="p-4 font-bold text-gray-600">Referrer Name</th>
                            <th class="p-4 font-bold text-gray-600">Referral Code</th>
                            <th class="p-4 font-bold text-gray-600 text-right">Actions</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y">
                        {% for c in codes %}
                        <tr class="hover:bg-gray-50 transition">
                            <td class="p-4 font-medium text-gray-800">{{ c.name }}</td>
                            <td class="p-4"><span class="bg-slate-100 px-3 py-1 rounded font-mono text-blue-600 font-bold">{{ c.code }}</span></td>
                            <td class="p-4 text-right">
                                <a href="/delete/{{ c.id }}" class="text-red-500 hover:text-red-700 font-bold text-sm bg-red-50 px-3 py-2 rounded-lg transition" 
                                   onclick="return confirm('Are you sure you want to delete this code?')">
                                    <i class="fas fa-trash-alt mr-1"></i> Delete
                                </a>
                            </td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
                {% if not codes %}
                <div class="p-10 text-center text-gray-400">
                    <i class="fas fa-folder-open text-4xl mb-3"></i>
                    <p>No codes added yet.</p>
                </div>
                {% endif %}
            </div>
        </div>
    </main>
</body>
</html>
